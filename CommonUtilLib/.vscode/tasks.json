{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Solution (Debug x64)",
            "type": "shell",
            "command": "$pf86 = [Environment]::GetFolderPath('ProgramFilesX86'); $vswhere = Join-Path $pf86 'Microsoft Visual Studio\\Installer\\vswhere.exe'; $msbuild = $null; if (Test-Path $vswhere) { $msbuild = (& $vswhere -latest -requires Microsoft.Component.MSBuild -find MSBuild\\**\\Bin\\MSBuild.exe | Select-Object -First 1) }; if (-not $msbuild) { $msbuild = (Get-Command MSBuild.exe -ErrorAction SilentlyContinue).Source }; if (-not $msbuild) { throw 'MSBuild.exe not found. Install Visual Studio Build Tools.' }; & $msbuild '${workspaceFolder}/CommonUtilLib.sln' '/p:Configuration=Debug' '/p:Platform=x64' '/m'",
            "args": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$msCompile",
            "detail": "Build the entire solution in Debug x64"
        },
        {
            "label": "Build Solution (Release x64)",
            "type": "shell",
            "command": "$pf86 = [Environment]::GetFolderPath('ProgramFilesX86'); $vswhere = Join-Path $pf86 'Microsoft Visual Studio\\Installer\\vswhere.exe'; $msbuild = $null; if (Test-Path $vswhere) { $msbuild = (& $vswhere -latest -requires Microsoft.Component.MSBuild -find MSBuild\\**\\Bin\\MSBuild.exe | Select-Object -First 1) }; if (-not $msbuild) { $msbuild = (Get-Command MSBuild.exe -ErrorAction SilentlyContinue).Source }; if (-not $msbuild) { throw 'MSBuild.exe not found. Install Visual Studio Build Tools.' }; & $msbuild '${workspaceFolder}/CommonUtilLib.sln' '/p:Configuration=Release' '/p:Platform=x64' '/m'",
            "args": [],
            "group": "build",
            "problemMatcher": "$msCompile",
            "detail": "Build the entire solution in Release x64"
        },
        {
            "label": "Run Tests",
            "type": "shell",
            "command": "$pf86 = [Environment]::GetFolderPath('ProgramFilesX86'); $vswhere = Join-Path $pf86 'Microsoft Visual Studio\\Installer\\vswhere.exe'; $vstest = $null; if (Test-Path $vswhere) { $vstest = (& $vswhere -latest -products * -find Common7\\IDE\\Extensions\\TestPlatform\\vstest.console.exe | Select-Object -First 1) }; if (-not $vstest) { $vstest = (Get-Command vstest.console.exe -ErrorAction SilentlyContinue).Source }; if (-not $vstest) { throw 'vstest.console.exe not found. Install Visual Studio test platform.' }; & $vstest '${workspaceFolder}/x64/Debug/MathTests.dll'",
            "args": [],
            "group": "test",
            "problemMatcher": [],
            "dependsOn": "Build Solution (Debug x64)",
            "detail": "Build and run unit tests"
        },
        {
            "label": "Run Matrix Tests",
            "type": "shell",
            "command": "$pf86 = [Environment]::GetFolderPath('ProgramFilesX86'); $vswhere = Join-Path $pf86 'Microsoft Visual Studio\\Installer\\vswhere.exe'; $vstest = $null; if (Test-Path $vswhere) { $vstest = (& $vswhere -latest -products * -find Common7\\IDE\\Extensions\\TestPlatform\\vstest.console.exe | Select-Object -First 1) }; if (-not $vstest) { $vstest = (Get-Command vstest.console.exe -ErrorAction SilentlyContinue).Source }; if (-not $vstest) { throw 'vstest.console.exe not found. Install Visual Studio test platform.' }; & $vstest '${workspaceFolder}/x64/Debug/MathTests.dll' '/TestCaseFilter:FullyQualifiedName~Matrix3x3Tests|FullyQualifiedName~Matrix4x4Tests'",
            "args": [],
            "group": "test",
            "problemMatcher": [],
            "dependsOn": "Build Solution (Debug x64)",
            "detail": "Build and run matrix-only tests"
        },
        {
            "label": "Run StackQueue Tests",
            "type": "shell",
            "command": "$pf86 = [Environment]::GetFolderPath('ProgramFilesX86'); $vswhere = Join-Path $pf86 'Microsoft Visual Studio\\Installer\\vswhere.exe'; $vstest = $null; if (Test-Path $vswhere) { $vstest = (& $vswhere -latest -products * -find Common7\\IDE\\Extensions\\TestPlatform\\vstest.console.exe | Select-Object -First 1) }; if (-not $vstest) { $vstest = (Get-Command vstest.console.exe -ErrorAction SilentlyContinue).Source }; if (-not $vstest) { throw 'vstest.console.exe not found. Install Visual Studio test platform.' }; & $vstest '${workspaceFolder}/x64/Debug/MathTests.dll' '/TestCaseFilter:FullyQualifiedName~StackQueueTests'",
            "args": [],
            "group": "test",
            "problemMatcher": [],
            "dependsOn": "Build Solution (Debug x64)",
            "detail": "Build and run stack/queue-only tests"
        },
        {
            "label": "Run Sudoku Tests",
            "type": "shell",
            "command": "$pf86 = [Environment]::GetFolderPath('ProgramFilesX86'); $vswhere = Join-Path $pf86 'Microsoft Visual Studio\\Installer\\vswhere.exe'; $vstest = $null; if (Test-Path $vswhere) { $vstest = (& $vswhere -latest -products * -find Common7\\IDE\\Extensions\\TestPlatform\\vstest.console.exe | Select-Object -First 1) }; if (-not $vstest) { $vstest = (Get-Command vstest.console.exe -ErrorAction SilentlyContinue).Source }; if (-not $vstest) { throw 'vstest.console.exe not found. Install Visual Studio test platform.' }; & $vstest '${workspaceFolder}/x64/Debug/MathTests.dll' '/TestCaseFilter:FullyQualifiedName~SudokuSolverTests'",
            "args": [],
            "group": "test",
            "problemMatcher": [],
            "dependsOn": "Build Solution (Debug x64)",
            "detail": "Build and run sudoku-only tests"
        },
        {
            "label": "Run Sudoku Demo",
            "type": "process",
            "command": "${workspaceFolder}\\SudokuDemo.exe",
            "args": [],
            "problemMatcher": [],
            "detail": "Run Sudoku demo and print solved board"
        },
        {
            "label": "Clean Solution",
            "type": "shell",
            "command": "$pf86 = [Environment]::GetFolderPath('ProgramFilesX86'); $vswhere = Join-Path $pf86 'Microsoft Visual Studio\\Installer\\vswhere.exe'; $msbuild = $null; if (Test-Path $vswhere) { $msbuild = (& $vswhere -latest -requires Microsoft.Component.MSBuild -find MSBuild\\**\\Bin\\MSBuild.exe | Select-Object -First 1) }; if (-not $msbuild) { $msbuild = (Get-Command MSBuild.exe -ErrorAction SilentlyContinue).Source }; if (-not $msbuild) { throw 'MSBuild.exe not found. Install Visual Studio Build Tools.' }; & $msbuild '${workspaceFolder}/CommonUtilLib.sln' '/t:Clean' '/p:Configuration=Debug' '/p:Platform=x64'",
            "args": [],
            "problemMatcher": "$msCompile"
        }
    ]
}
